[project]
name = "semantic-dataset-router"
version = "0.1.0"
description = "RAG router for CSV datasets."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.2",
    "openai>=2.18.0",
    "pandas>=3.0.0",
    "pydantic-settings>=2.12.0",
    "rich>=14.3.2",
    "sentence-transformers>=5.2.2",
    "typer>=0.21.1",
]
